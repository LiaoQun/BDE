_base_: ../base.yaml

# Default experiment: no CV, Method-B (val_size=0.1 used for early stopping).
data:
  base_data_paths:
    - examples/test_data.csv.gz
  cross_validation: none
  val_size: 0.1

train:
  epochs: 5